name: discord-notify.yml (mirror-inert)

# GitHub is a read-only mirror. LastGit is source of truth for CR/CI/merge.
on:
  workflow_dispatch:

jobs:
  inert:
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub mirror — no Actions. Use LastGit (lastdb:///discord-notify.yml_SLUG) + code-node forge."

